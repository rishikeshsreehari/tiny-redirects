backend:
  name: github
  repo: rishikeshsreehari/tiny-redirects
  branch: main

collections:
  - name: 'redirects'
    label: 'Redirect Files'
    folder: 'redirects'
    create: true
    extension: 'yaml'
    format: 'yaml'
    slug: '{{slug}}'
    summary: '{{filename}}'
    fields:
      - label: 'Redirects'
        name: 'body'
        widget: 'code'
        default_language: 'yaml'
        hint: 'Paste raw redirect list here.'
        default: |
          - short_url: "/example"
            target_url: "https://example.com"
            status_code: 301
